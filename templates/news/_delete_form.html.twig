<form method="post" action="{{ path('app_news_admin_delete', {'id': news.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ news.id) }}">
    <button class="btn btn-primary mb-2">Supprimer</button>
</form>
