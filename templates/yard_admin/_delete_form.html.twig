<form method="post" action="{{ path('app_yard_delete', {'id': yard.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ yard.id) }}">
    <button id="delete_btn" class="btn">Supprimer</button>
</form>
